/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["xumm.app", "app.roogsino.io", "crocoapi.oraicasino.io","cdn.softswiss.net"], // Add the domain where your images are hosted
  },
};

export default nextConfig;
